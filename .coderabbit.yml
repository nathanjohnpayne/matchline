# .coderabbit.yml — CodeRabbit configuration
# See https://docs.coderabbit.ai/configuration

language: en-US

tone_instructions: >-
  Focus on bugs, security issues, and correctness over style nitpicks.
  Flag any hardcoded credentials, exposed API keys, or secrets.
  Keep comments actionable and concise.

reviews:
  auto_review:
    enabled: true
    drafts: false
    base_branches:
      - main

  profile: assertive
  request_changes_workflow: false

  high_level_summary: true
  high_level_summary_placeholder: "@coderabbitai summary"
  high_level_summary_in_walkthrough: true
  collapse_walkthrough: true
  poem: false

  related_issues: true
  related_prs: true
  assess_linked_issues: true
  estimate_code_review_effort: true

  path_instructions:
    - path: "scripts/**"
      instructions: >-
        Review shell scripts for safety: unquoted variables, command
        injection risks, and cross-platform portability issues.
        Error handling: prefer `set -euo pipefail`, but accept
        `set -eo pipefail` (no `-u`) where the script deliberately
        relies on unset-variable expansion (e.g., optional env vars,
        op-preflight credential overrides) — flag only when an unset
        variable would actually cause silent misbehavior.
    - path: "docs/**"
      instructions: >-
        Check documentation for accuracy against the actual scripts and
        configuration it describes. Flag stale references.

chat:
  auto_reply: true

knowledge_base:
  learnings:
    scope: auto
