{
  "name": "matchline-functions",
  "version": "0.0.0",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "~5.7.0"
  }
}
